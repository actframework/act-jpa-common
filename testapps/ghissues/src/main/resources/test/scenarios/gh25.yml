Scenario(GH25):
  description: "[GH25] JPAQuery - set limit does not work on MS Sql server"
  fixtures:
    - load-foo-25
  interactions:
  - description: Test
    request:
      get: /25
    response:
      json:
        weight:
          - gt: 10
